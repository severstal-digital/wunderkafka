version: 2

build:
  os: ubuntu-20.04
  languages:
    python: "3.8"
  apt_packages:
    - libkrb5-dev

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
   configuration: docs/conf.py
